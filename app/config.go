package app

// Application config
var (
	AppName        = "swarm-cronjob"
	AppDescription = "Create jobs on a time-based schedule on Swarm"
	AppPackage     = "github.com/crazy-max/swarm-cronjob"
	AppUrl         = "https://" + AppPackage
	AppVersion     = "0.1.0"
)
